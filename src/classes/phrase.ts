class Phrase {
    id: number
    author: string
    sentence : string

    constructor (id: number, author: string,sentence: string) {
        this.id = id
        this.author = author
        this.sentence = sentence
    }
}